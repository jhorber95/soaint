version: '2'
services:
  soaint-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/SOAINT/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=SOAINT
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
